<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.AdminOrder">

	<select id="selectAdminOrderList" resultType="csOrderVO" parameterType="map">
			select * 
			from product_order
		<if test="searchField != '' ">
			where ${searchField} like concat('%', #{keyword}, '%')
		</if>
			order by order_idx desc
			limit #{pageFirst}, #{pageSize};
	</select>

	<select id="countOrderList" resultType="int">
			select count(*) 
			from product_order
		<if test="searchField != '' "	>
			where ${searchField} like concat('%', #{keyword}, '%');
		</if>
	</select>

	<!-- 주문별 정보 조회 -->
<!-- 	<select id="selectEachOrderList" parameterType="int" resultType="csOrderVO"> -->
<!-- 		select *  -->
<!-- 		from product_order -->
<!-- 		where order_idx = #{order_idx}; -->
<!-- 	</select> -->

</mapper>