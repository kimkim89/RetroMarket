<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="mapper.Product">
	
	<!--  -->
	<select id="selectAllProducts" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 		
		order by a.mk_register_date desc;	
	</select>
	
	<select id="selectSnack" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 1 
		order by a.mk_register_date desc;	
	</select>
	
	<select id="selectJellyCandy" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 2 
		or a.mk_product_category = 3
		order by a.mk_register_date desc;	
	</select>
	
	<select id="selectEtc" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 4 
		order by a.mk_register_date desc;	
	</select>
	
	<!-- 상품 상세 페이지 select -->
	<select id="selectEachProd" parameterType="String" resultType="java.util.HashMap">
		select * 
			from product_info as a left join product_file as b 
			on a.mk_product_id = b.pf_product_id 
			where a.mk_idx = #{product_id}
			order by a.mk_register_date desc;
	</select>
	
		
	<!--  20210811 수정 작업 진행 시작 -->
	
	<select id="selectAllPopularProd" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 4 
		order by a.mk_register_date desc;	
	</select>

	<select id="selectAllNewProd" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 4 
		order by a.mk_register_date desc;	
	</select>
	
	<select id="selectAllDiscountProd" resultType="java.util.HashMap">		
		select * 
		from product_info as a left join product_file as b 
		on a.mk_product_id = b.pf_product_id 
		where a.mk_product_category = 4 
		order by a.mk_register_date desc;	
	</select>

	<!--  20210811 수정 작업 진행 끝 -->
	
</mapper>