<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="mapper.Member">

	<!-- 회원 가입 -->
	<insert id="userJoin" parameterType="memberVO"> 
		insert into member (id, name, nickname, phone, pwd, email, address1, address2)
		values (#{id}, #{name}, #{nickname}, #{phone}, #{pwd}, #{email}, #{address1}, #{address2})
	</insert>
	
	<!-- ID중복 확인 -->
	<select id="idcheck" resultType="_int">
		select count(id) from member where id = #{id}
	</select>

	<!-- 닉네임 중복 확인 -->
	<select id="nicknameCheck" resultType="_int">
		select count(nickname) from member where nickname = #{nickname}
	</select>
	
	
	
	
	
	
	
	
	
	
</mapper>